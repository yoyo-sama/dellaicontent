# Leçons & pièges (durement acquis — lire avant de toucher aux graphes)

Chaque point ci-dessous a été découvert par un échec réel puis validé par un rendu GPU. Ne pas re-deviner : réutiliser.

## Modèles & appariements (installés sur ce GB10)

| Usage | Modèle | Encodeur / VAE | Réglages validés |
|---|---|---|---|
| Image qualité | `flux-2-klein-9b-fp8` (UNETLoader) | **`qwen_3_8b_fp8mixed`** type `flux2` + `flux2-vae` | 4 steps, cfg 1, euler, négatif = ConditioningZeroOut(positif), sigmas `Flux2Scheduler` |
| Image rapide + enrichisseur intégré | `ernie-image-turbo` | `ministral-3-3b` type flux2 + prompt-enhancer + `flux2-vae` | 8 steps cfg 1 (turbo) / 20 steps cfg 4 (base) |
| Exploration | `z_image_turbo_bf16` | `qwen_3_4b` + `ae` | 8 steps cfg 1 |
| Édition/localisation | `qwen_image_edit_2509_fp8_e4m3fn` | `qwen_2.5_vl_7b_fp8_scaled` + `qwen_image_vae` + LoRA `Qwen-Image-Edit-2509-Lightning-4steps` | 4 steps cfg 1 |
| Vidéo (+audio) | checkpoint `ltx-2.3-22b-distilled-1.1` (fusionné dev+LoRA, **sans** LoRA séparé) | text encoder `gemma_3_12B_it_fp4_mixed` via `LTXAVTextEncoderLoader` | `SamplerEulerAncestral(eta 0)` + `ManualSigmas` "1.0, 0.99375, 0.9875, 0.98125, 0.975, 0.909375, 0.725, 0.421875, 0.0" (8 steps distillés), cfg 1, 25 fps |

> **NOTE 2026-09-01 (LOT 7) — le tableau ci-dessus est désormais HISTORIQUE pour 4 de ses lignes.**
> Sur décision utilisateur, **seuls Krea 2 (image), LTX 2.5 (vidéo) et Minimax H3 (vidéo)** sont
> utilisables dans l'app. `flux-2-klein-9b-*`, `ernie-image*`, `z_image_turbo_bf16` et
> `ltx-2.3-22b-distilled-1.1` ne sont plus référencés nulle part dans `index.html` ni
> `workflows/manifest.json` (builders JS supprimés, templates API supprimés). Les lignes restent
> dans le tableau à titre de mémoire technique (appariements encodeur/VAE, pièges n°1/n°3/n°4/n°5)
> — **ne pas les réintroduire comme générateurs**. Seule exception validée : la ligne
> **Édition/localisation `qwen_image_edit_2509`** reste pleinement active (Qwen-Edit est un modèle
> d'édition, pas un concurrent de Krea 2) — LoRA à charger sous `Qwen/Qwen-Image-Edit-2509-Lightning-4steps-V1.0-bf16.safetensors`.
> Réglages en vigueur : Krea 2 = 8 steps / cfg 1 ; LTX 2.5 = `LTXVDualCFGGuider` + `ManualSigmas`
> 8 steps distillés, **24 fps** (et non 25) ; Minimax H3 = LoRA turbo ON, 8 steps.

**Piège n°1 — encodeur Flux2** : les templates officiels "klein" sont en 4B et référencent `qwen_3_4b`. Avec le 9B installé, ça produit un conditioning 7680-dim (3×2560) là où le modèle attend 12288 (3×4096) → `mat1 and mat2 shapes cannot be multiplied` dans `txt_in`. Toujours `qwen_3_8b_fp8mixed` avec le 9B.

**Piège n°2 — LoRA Qwen** : seul `Qwen-Image-Edit-2509-Lightning-4steps-V1.0-bf16` est installé (PAS `Qwen-Image-Lightning-4steps-V1.0`).

## Vidéo LTX 2.3

- **Checkpoint distillé fusionné partout** (décision + A/B utilisateur 2026-07-07) : `ltx-2.3-22b-distilled-1.1.safetensors` (dev + LoRA distillé intégrés) sur **tous** les graphes vidéo, **sans** le LoRA `ltx_2.3_22b_distilled_1.1_lora_dynamic…` séparé (double distillation sinon). A/B validé en rendu réel : frames cohérentes 8/10 (ltx_t2v, ltx_i2v, builder FLF2V) + audio. L'ancien setup `ltx-2.3-22b-dev-fp8` + `LoraLoaderModelOnly(…, 0.5)` est obsolète. Note : aucun template officiel n'utilise ce checkpoint fusionné — c'est un choix studio validé par rendu, pas une recette officielle.

- **FLF2V (first/last frame)** par segment : `LTXVPreprocess(img_compression 35)` ×2 → `LTXVAddGuide(frame_idx 0, strength 0.7)` → `LTXVAddGuide(frame_idx -1, 0.7)` → sampler sur le latent AddGuide2[2] → **sortie sampler slot 1 (`denoised_output`)** → `LTXVCropGuides` (conditioning depuis AddGuide2) → `VAEDecodeTiled(768/64/4096/64)`.
- **Piège n°3 — recadrage sauvage** : `LTXVAddGuide` center-croppe/zoome toute image guide dont le ratio ≠ latent → résultat "sans rapport" avec l'image. Toujours insérer `ImageScale(lanczos, width, height, crop:"center")` avant `LTXVPreprocess`, et dériver le format vidéo de la 1ʳᵉ image (côté long ≤1280, multiples de 32).
- **Piège n°4 — prompt de mouvement** : en FLF2V le texte doit décrire le MOUVEMENT ; un prompt de contenu (ex. prompt de poster) fait dériver le milieu des segments vers ce contenu (effet "diaporama d'images inventées"). Segments courts (2–3 s) = fidélité ; longs (5 s+) = dérive.
- Frames valides : `8n+1` (ex. 49 ≈ 2 s @ 25 fps ; formule `8*round((s*25-1)/8)+1`).
- La hauteur/largeur est arrondie au multiple de 32 inférieur au rendu (720 → 704) : normal.
- **Audio** : AUCUN modèle supplémentaire requis — `LTXVAudioVAELoader(ckpt_name="ltx-2.3-22b-distilled-1.1.safetensors")` charge l'audio VAE depuis le checkpoint (le "vocoder" listé par certains templates n'est pas nécessaire au chemin standard). Câblage : `LTXVEmptyLatentAudio(frames, 25, 1, audio_vae)` → `LTXVConcatAVLatent(video_latent, audio_latent)` AVANT le sampler → `LTXVSeparateAVLatent(sampler[1])` → slot 0 = vidéo (vers CropGuides/decode), slot 1 → `LTXVAudioVAEDecode` → `CreateVideo.audio` (entrée optionnelle). Multi-segments : chaîner les AUDIO avec `AudioConcat(direction:"after")`.
- Le template t2v officiel est en 2 passes (basse rés. + `LTXVLatentUpsampler` ×2, upscaler `ltx-2.3-spatial-upscaler-x2-1.1`) — réutiliser `workflows/api/ltx_t2v.json` plutôt que le recoder. Le template i2v (`ltx_i2v.json`) en est dérivé : un `PrimitiveBoolean` active le guide `LTXVImgToVideoInplace` (bypass pour t2v, actif pour i2v). Un graphe i2v single-pass ad hoc sort mal à cfg 1 sans passe de refine — toujours dériver l'i2v du t2v validé.
- **Piège n°5 — adhérence au prompt** : le pipeline distillé tourne à **cfg 1** (`CFGGuider`), donc pas de guidance classifier-free et negative prompt ignoré. L'adhérence repose sur le **prompt-enhancer intégré** du template officiel : `LoraLoader(gemma-3-12b-it-abliterated_lora_rank64_bf16, 1, 1)` sur l'encodeur → `TextGenerateLTX2Prompt(max_length 2048, sampling on, temp 0.7)` → texte enrichi vers le `CLIPTextEncode` positif (encodé avec le clip SANS LoRA ; la sortie model du LoraLoader est inutilisée). L'avoir omis = vidéos plates et prompts non respectés (constat utilisateur 2026-07-07). Présent dans : templates api t2v/i2v (`enrich:"builtin"` au manifest → gemma4 court-circuité), `addLtxEnhance` par segment FLF2V, et les workflows drag-drop `storyboard_animatic.json` (5 enhancers) / `campaign_generator.json` (teaser). Ne jamais le retirer d'un graphe LTX.

## API & graphes ComfyUI

- **Piège n°5 — slots `ComfyMathExpression`** : slot 0 = FLOAT, slot 1 = INT. Une entrée INT (ex. `length`) câblée sur le slot 0 → `return_type_mismatch`.
- **Piège n°6 — ids non numériques** : certains templates API contiennent des clés comme `"PH"`. Pour générer de nouveaux ids : `Math.max(0, ...keys.map(Number).filter(Number.isFinite)) + 1`.
- `ImageStitch(match_image_size:true)` étire l'image la plus petite : équilibrer les lignes d'une grille (4 → 2+2, 5 → 3+2) sinon une ligne courte devient géante.
- Format UI : links du graphe principal = tableaux `[id,src,slot,dst,slot,type]` ; dans `definitions.subgraphs` = dicts (`origin_id`/`target_id`) et les ids **-10/-20** sont les nœuds frontière entrée/sortie (pas des liens cassés). Types "virtuels" absents d'object_info mais valides : `MarkdownNote`, `Note`, `Reroute`.
- Ordre des `widgets_values` (format UI) : inputs widget du schéma dans l'ordre required+optional ; tout INT avec `control_after_generate:true` est suivi d'un widget parasite ("randomize"/"fixed") à ignorer. COMBO en deux styles (liste littérale vs `type:"COMBO"`+`extra.options`). Géré par `tools/convert.py`.
- Templates officiels de référence : dans le conteneur `comfyui-nvidia` sous `/comfy/mnt/venv/lib/python3.12/site-packages/comfyui_workflow_templates_media_{image,video,other}/templates/`. **Toujours partir d'eux** pour un nouveau modèle, jamais de mémoire.

## Storyboard v2 (charsheet + locsheet Ernie + keyframes Qwen-Edit ancrées double + cuts LTX i2v)

- **Piège n°9 (CRITIQUE) — les cuts LTX i2v dérivent fréquemment vers un contenu SANS RAPPORT avec la keyframe/le personnage** : testé en rendu réel sur les DEUX runs de qualification (N=8 du 2026-07-17, frames extraites au milieu de chaque cut, pas seulement à la première/dernière image) — sur 9 cuts (8 plans + tenue), seuls 3 gardent clairement le personnage et le décor (cuts 4, 5, 9), 2 sont ambigus/partiels (cuts 2, 6 — un intrus au premier plan pour le 6), et **4 dérivent totalement** vers un contenu générique sans rapport (cut 1 → fontaine ornementale, cut 3 → homme en costume dans une rue, cut 7 → homme agenouillé près d'une fontaine, cut 8 → champ de blé). Sur le run N=4 angles, les 4 cuts dérivent presque tous (ex. une pomme sur fond blanc, un mur de roses) — voir frames dans le rapport de qualification LOT 3. **Cause probable** : `submitCutJob` passe **uniquement** `shot.motion` (ex. "Slow crane rise upwards revealing scale") comme prompt — une phrase de mouvement de caméra courte, sans aucun mot de scène/personnage. Le template `api/ltx_i2v.json` (`enrich:"builtin"`) route ce texte dans l'enhancer natif LTX (`TextGenerateLTX2Prompt`, node texte **sans aucune entrée image** — confirmé en lisant le graphe : il ne reçoit que `clip`+`prompt`) qui élabore alors une scène complète **à partir de rien**, sans connaître le contenu de l'image de référence. Comme le pipeline distillé tourne à **cfg 1** (piège n°5 déjà documenté : l'adhérence dépend entièrement de ce texte enrichi), quand l'enhancer invente une scène sans rapport, le rendu suit cette scène inventée plutôt que l'image-guide — qui n'ancre fiablement que la toute première frame. Ce piège est différent (et plus grave) du problème d'angle Qwen-Edit ci-dessous : il touche la vidéo, pas la keyframe, et affecte la promesse même de l'"animatic" (montrer le mouvement de LA scène décrite). **CORRIGÉ (2026-07-17)** : `submitCutJob` ne passe plus `shot.motion` seul mais un prompt qui ancre l'enhancer sur le contenu réel de la keyframe — scène du plan + fiche personnage + mouvement de caméra : `` `${scene}. ${charDesc}. Camera motion: ${motion}.` ``. La fonction reçoit désormais `scene` et `charDesc` en paramètres ; ses DEUX appelants ont été adaptés (mode Auto `generateStoryboardV2` : `charDesc` local + `scenes = shots.map(s => s.scene)` ; mode Réalisateur `directorStep3` : `charDesc` destructuré de l'état `director` + même map de scènes). Le cut de tenue finale reçoit le même ancrage (scène du dernier plan + `charDesc` + « Locked camera holding on the final shot »). **Pattern validé en rendu réel** : ré-rendu des cuts 1, 3, 8 du run N=8 (ceux qui avaient dérivé — fontaine / homme en costume / champ de blé) sur leurs keyframes existantes ; frame médiane extraite et inspectée pour chacun → le personnage (astronaute rousse en combinaison blanche/orange) ET le décor de la keyframe (ruines fleuries de violet, arche envahie de végétation, silhouette au coucher de soleil) sont bien présents au milieu du clip, plus aucune dérive. La leçon générale : à cfg 1, tout node texte enrichi sans entrée image doit recevoir une description de la scène cible, pas seulement une consigne de mouvement — sinon il invente. Voir aussi piège n°4 (prompt de contenu vs mouvement en FLF2V), même racine. **ÉVOLUTION (ancrage double, même jour, plus tard dans la journée)** : depuis l'ajout d'une fiche décor structurée gemma (`locationSheetFromBrief` → `locDesc`), `submitCutJob` ancre le prompt du cut sur scène + personnage + **décor** : `` `${scene}. ${charDesc}. ${locDesc}. Camera motion: ${motion}.` `` — `locDesc` supplémentaire par rapport au correctif précédent. Objectif : éviter qu'en l'absence de mention explicite du décor, l'enhancer LTX ne glisse vers un arrière-plan générique différent de celui de la keyframe alors même que le personnage reste correct. Validé en rendu réel LOT 4 (run N=4, brief astronaute) : voir verdict et frames médianes plus bas dans ce document et dans `docs/ARCHITECTURE.md`.

- **Piège n°10 — dans un template Qwen-Edit dual, l'image 2 doit rester un conditioning pur, jamais le latent de départ** : inspection du graphe `workflows/api/qwen_edit_dual.json` — l'image 1 (`{{IMAGE}}`, nœud `78`→`433:117`→`FluxKontextImageScale`) alimente à la fois `TextEncodeQwenImageEditPlus` (positif ET négatif, comme `image1`) ET `VAEEncode` (`433:88`) dont la sortie devient `latent_image` du `KSampler` — c'est donc elle qui fixe la géométrie/pose de départ du denoise. L'image 2 (`{{IMAGE2}}`, nœud `79`→`433:118`) n'alimente QUE `TextEncodeQwenImageEditPlus` comme `image2` (positif et négatif) : elle influence le style/contenu via le conditioning texte-image mais ne fournit aucun latent. Conséquence pratique confirmée en rendu (LOT 4, N=4) : le personnage garde la pose/perspective héritée de la charsheet (image 1) pendant que le décor de la locsheet (image 2) est repris fidèlement dans le style/l'agencement — sans jamais produire de split-screen ni de collage des deux planches (aucune ligne de partage visible sur les 4 keyframes testées), confirmant que `TextEncodeQwenImageEditPlus` compose réellement les deux références en une seule scène plutôt que de les juxtaposer. Si un futur ancrage a besoin que le DÉCOR pilote la géométrie de départ (ex. angle de caméra imposé par le lieu plutôt que par le personnage), il faudrait inverser quel `LoadImage` alimente `VAEEncode` — pas une simple bascule `anchor`, un changement du template.
- **Planches multi-panneaux Ernie réussies du premier coup, y compris la fiche décor** : comme pour la charsheet (piège n°7), la locsheet (`buildLocsheetGraph`) a produit dès le premier rendu (LOT 4, N=4) une planche pleine cadre, sans texte/légende/collage, avec plusieurs angles du même lieu + gros plans de matières — même recette (positif descriptif détaillé + négatif `LOCSHEET_NEG` = `CHARSHEET_NEG` + bannissement explicite des personnes, cfg 4 → le négatif agit). Confirme que la combinaison "contraintes positives denses + négatif actif à cfg 4" généralise à un sujet différent (lieu vs personnage) sans réglage supplémentaire.

- **Verdict qualification LOT 4 (run dual complet N=4, brief astronaute, 2026-07-17 22:49→23:03)** — comparaison avant/après par axe, chemins sous `/home/sparks/comfyui-spark/basedir/output/studio/story/` sauf mention contraire :
  - **(a) Fidélité du visage en gros plan** : AVANT (`key_02_00003_.png`, `key_04_00003_.png`, run mono-ancre du matin) — cheveux roux reconnaissables mais design du casque/combinaison incohérent d'un plan à l'autre (casque gris style NASA sur `key_01_00003_.png` vs combinaison blanc/orange sportive sur `key_04_00003_.png`) : même le personnage dérive sans ancrage image fixé. APRÈS — portrait en gros plan de `charsheet_00009_.png` (casque blanc à liseré orange, mèche rousse, visage identique) retrouvé à l'identique sur les 4 keyframes (`key_01_00009_.png` … `key_04_00009_.png`), avec `key_03_00010_.png` en plan rapproché du visage confirmant le match trait pour trait. Verdict : fidélité forte, confirmée par comparaison directe portrait charsheet ↔ 4 keyframes.
  - **(b) Constance du décor entre les 4 cases** : AVANT (`key_01_00003_.png` à `key_04_00003_.png`) — quatre lieux **sans aucun rapport** : épave métallique/gravier, ruines façon colonnade romaine avec texte gravé sur la pierre, désert rocheux au crépuscule (vue de dos), désert rouge façon Mars avec planète à anneaux visible. Décor porté par le texte seul = zéro constance. APRÈS — `locsheet_00005_.png` (ruines dans la jungle, monolithes de pierre, lumière de fin de journée) retrouvée à l'identique sur `key_01_00009_.png` … `key_04_00009_.png` (mêmes monolithes, même végétation, même lumière). Verdict : constance forte, confirmée par comparaison directe locsheet ↔ 4 keyframes.
  - **(c) Dérive des cuts vidéo (frame médiane, `ffmpeg select=eq(n\,24)`)** : AVANT correctif (run N=8, prompt = `shot.motion` seul) — `/tmp/claude-1000/-home-sparks/bbffbcbd-6947-4798-ac06-63f948506038/scratchpad/lot3/n8_cutframes/cut_01_00003__mid.png` = fontaine ornementale, aucun personnage ; `cut_03_00003__mid.png` = homme en costume souriant dans une rue européenne — deux contenus sans rapport avec la keyframe source (confirme piège n°9). Après correctif simple (personnage seul, sans `locDesc`) — `/tmp/claude-1000/-home-sparks/bbffbcbd-6947-4798-ac06-63f948506038/scratchpad/fix-cuts/mid_cut_01.png` (astronaute dans un décor de cristaux violets fantaisistes) vs `mid_cut_03.png` (astronaute dans une arche de pierre couverte de lierre) : personnage tenu, mais décor différent d'un cut à l'autre faute d'ancrage décor dans le prompt. APRÈS ancrage double complet (LOT 4) — frames médianes extraites dans `/tmp/claude-1000/-home-sparks/bbffbcbd-6947-4798-ac06-63f948506038/scratchpad/lot4-qualif/after_mid/cut_0{1,2,3,4,5}_…_mid.png` : personnage ET décor (ruines dans la jungle) tenus sur les 5 cuts (4 plans + tenue), aucune dérive observée. Verdict : dérive résolue sur ce run par l'ajout de `locDesc` au prompt du cut (0/5 dérivant vs 4/9 avant correctif initial sur le run N=8).
- **Piège n°7 — vocabulaire "storyboard/keyframe" dans un prompt Qwen-Edit** : demander à Qwen-Image-Edit 2509 de produire une "storyboard keyframe" (ou tout vocabulaire évoquant une planche) le fait dessiner une véritable planche annotée — cadre noir, marges, légendes/texte incrusté — au lieu d'une scène pleine image. Comme le pipeline tourne à **cfg 1** (LoRA Lightning 4 steps), le negative prompt est ignoré : la seule parade est de tout formuler **positivement** dans le prompt lui-même. `keyframePrompt()` (index.html) impose donc explicitement "one photorealistic cinematic film still", "extends all the way to all four edges", "no black frame, no margin, no storyboard panel, no caption and no text anywhere" — validé en rendu réel (grille 8 cases 2026-07-17 : aucune planche/légende parasite, plein cadre sur les 8 cases).
- **Piège n°8 — l'ancrage Qwen-Edit ne respecte PAS les angles de caméra forts** : testé en rendu réel (run qualification N=4, 2026-07-17, angles imposés dans le prompt `angle`) — plongée zénithale ("bird's eye view, directly overhead") et contre-plongée extrême ("worm's eye view, looking almost straight up") sont **ignorées** : Qwen-Edit conserve la composition/pose "vue de face, plein pied" de l'image d'ancrage (la charsheet) quel que soit le texte d'angle, alors qu'il respecte bien un **gros plan** (zoom/recadrage) et un **plan large** (léger recul, mais le sujet reste central et dominant plutôt que "minuscule dans le paysage"). Explication probable : un modèle d'édition d'image conditionne fortement sur la structure/perspective de l'image de référence (contrairement à un modèle de génération pure) — un changement de hauteur/angle de caméra revient à redessiner toute la géométrie de la scène, ce que l'édition instruction-based résiste à faire même à `denoise=1`. Aucune alternative d'ancrage mieux adaptée n'est installée sur ce GB10 (seul `qwen_image_edit_2509_fp8_e4m3fn` est un vrai modèle d'édition ; `flux1-fill-dev` est de l'inpainting par masque, pas conçu pour une réinvention de perspective et poserait le même problème). En pratique le risque reste limité : `shotListFromBrief` (gemma) ne propose jamais d'angle aussi extrême de lui-même (repli statique `SHOT_ANGLES` : wide/low-angle/medium/close-up/over-the-shoulder/dutch/high-angle/tracking — rien de zénithal ni de vue-de-ver) ; le problème ne se manifeste que si un angle extrême est forcé à la main (mode Réalisateur, édition du champ "Angle"). À corriger uniquement si le besoin apparaît : documenter la limite dans l'UI, ou étudier un ancrage par conditionnement différent (ex. IP-Adapter/consistency LoRA sur un modèle de génération pure plutôt qu'un modèle d'édition) — changement d'architecture, pas une bascule `anchor` d'une ligne.
- **Confirmation piège n°`8n+1` au-delà du FLF2V** : le template `api/ltx_i2v.json` calcule ses frames en graphe (`a*b+1` = durée×fps+1, ex. 2s×25+1 = 51) mais le rendu réel produit **49 frames** (`ffprobe` : `nb_read_frames=49`, `8*6+1`) — la troncature au 8n+1 valide est silencieuse quelque part dans les nœuds LTXV, même quand le graphe demande une valeur différente. Conséquence pratique validée : l'animatic assemblé en cuts francs a exactement `N_cuts × frames_par_cut` images (9 cuts × 49 = 441 frames pour un run N=8, vérifié `ffprobe` sur l'animatic final) — la somme est bien exacte, mais sur la base des frames **réellement rendues**, pas de la valeur brute calculée par `{{DURATION}}×fps+1`.
- **Observation (lecture de code, non testée en isolation)** : le brief par défaut du scénario "Storyboard" (`SCENARIOS.storyboard.prompt` dans index.html) commence encore par "Storyboard keyframe: a detective enters…" — un phrasé de plan UNIQUE hérité de l'ancien pipeline `storyboard_full` (Flux2 seul, supprimé). Si un utilisateur bascule ce scénario puis passe direct au pipeline `storyboard_v2` sans réécrire le brief, `characterSheetFromBrief`/`shotListFromBrief` reçoivent ce texte comme "scène" — gemma devrait normalement s'en sortir (son rôle est justement d'en extraire personnage/plans) mais le phrasé "wide establishing shot" en fin de texte pourrait biaiser tous les plans vers un même cadrage. Non vérifié en rendu (hors périmètre de cette qualification) — à garder à l'œil si le prochain lot touche aux scénarios par défaut.

## Canvas / litegraph — 2026-09-08

- **Piège n°17 — un combo litegraph construit avec un tableau déjà résolu (`.map(...)` figé) ne voit jamais une liste peuplée après coup par un fetch réseau.** En retirant la curation manuelle des LoRA (`KREA2_LORAS`/`H3_STYLE_LORAS` de `js/engine.js`, désormais peuplées de façon asynchrone par `fetchLoraOptions()` au chargement du module), les combos "style (LoRA)" de `js/nodes-simple.js`/`js/nodes-advanced.js` sont restés bloqués sur la liste vide initiale (`["", "Aucun"]`) même une fois le fetch résolu — le nœud litegraph avait déjà capturé `{ values: E.KREA2_LORAS.map(l => l[0]) }` au moment de sa construction (`addWidget("combo", …)`), et cette copie ne se met jamais à jour même si le tableau source, lui, est ensuite muté (`push`). Cause trouvée en lisant le code source de `litegraph.js` lui-même (pas dans sa doc) : le cas `"combo"` du handler `pointerdown` du widget relit `widget.options.values` **au moment du clic**, et si cette propriété est déjà une **fonction**, il l'appelle à cet instant plutôt que d'utiliser une valeur figée — mais si c'est un tableau, il l'utilise tel quel, sans jamais le relire ailleurs. **Correction : passer une fonction (`values: () => E.KREA2_LORAS.map(l => l[0])`, ou une référence de fonction nommée type `loraIds`/`h3LoraIds`) plutôt qu'un tableau pré-calculé**, dès qu'un combo doit refléter une source qui peut changer après la construction du nœud. Ce mécanisme est spécifique aux combos litegraph (Canvas) — un `<select>` DOM classique (Studio, `index.html`) n'a pas cette relecture automatique et doit être repeuplé explicitement par un rendu appelé quand la source change (`renderLoraSelect()`).

## Application

- Les fetch de `manifest.json` et des templates utilisent `cache:"no-store"` — indispensable, sinon le navigateur sert des versions périmées après modification côté serveur.
- gemma4:e4b : appels `/api/chat` avec `format:"json"` + `think:false` (retry sans `think` si 4xx). Ses réponses JSON peuvent contenir des objets là où on attend des chaînes → toujours normaliser. Premier appel à froid ≈ 45 s (chargement du modèle).
- gpt-image-1 (OpenAI) et gemini-2.5-flash-image acceptent le CORS navigateur ; Higgsfield non (intégration via MCP côté agent uniquement).
- ComfyUI n'a pas d'API de suppression d'outputs → la "suppression" galerie est un masquage persistant (localStorage).

## Nouveaux modèles LOT 1 (Krea 2 / LTX 2.5 / Minimax H3) — 2026-09-01

- **Piège n°11 — la longueur vidéo Minimax H3 se cale sur `17n+5`, pas sur le `8n+1` de LTX** : les trois templates H3 (`api/minimax_h3_{t2v,i2v,r2v}.json`) calculent leur `length` par `ComfyMathExpression` `max(5, round(a * 24)) + (5 - (max(5, round(a * 24)) % 17)) % 17`. Une demande de 1 s à 24 fps ne donne donc **pas 24 frames mais 39** (`17*2+5`), soit **1,625 s** — vérifié en rendu réel sur les 9 mp4 H3 du lot (`ffprobe nb_read_frames=39`, `duration=1.625` sur `h3_t2v_base20`, `h3_t2v_turbo{4,6,8}`, `h3_r2v_turbo{4,6,8}`, `h3_r2v_face8`, `h3_i2v_probe`). La durée réellement rendue peut donc dépasser de plus de 60 % la durée demandée sur les clips courts. Conséquence pratique : ne jamais additionner des `{{DURATION}}` pour prévoir la durée d'un assemblage H3 — toujours passer par la grille 17n+5 (ou `ffprobe` a posteriori), exactement comme pour le 8n+1 LTX déjà documenté, mais avec une grille différente. Les dimensions, elles, sont arrondies au multiple de **32 inférieur** (`a - a % 32`), pas 8 ni 64.

- **Piège n°12 — la LoRA turbo Minimax H3 en dessous de 6 steps casse la colorimétrie (et non la cohérence)** : comparaison en rendu réel de `h3_t2v_base20` (LoRA OFF, 20 steps) contre `h3_t2v_turbo{4,6,8}` (LoRA `loras/H3/minimax_h3_turbo_v4_step600_ema_pruned_comfyui.safetensors`, `strength_model 1`), frames 0/19/38 extraites et regardées (planches sous `/home/sparks/comfyui-spark/basedir/output/validate/lot1/h3_t2v_*.png`). À **4 steps** la structure et le mouvement restent parfaitement cohérents — c'est le rendu colorimétrique qui s'effondre : aplats saturés « clippés » (le ciré jaune devient fluo sans modelé), dominante cyan sur les gris, ciel posterisé en bandes, micro-détails effacés. Sur le pipeline **r2v** le même réglage ajoute un défaut plus grave et facile à manquer si l'on ne regarde que le centre de l'image : **une bande de couleurs parasites (traînée arc-en-ciel) en bas de cadre** sur toute la durée du clip (`validate/lot1/h3_r2v_turbo4_f{0,19,38}.png`), absente à 6 et 8 steps. À **6 steps** la colorimétrie est saine mais les matières restent lissées ; à **8 steps** le rendu est visuellement au niveau du 20-steps sans LoRA (densité du jaune, détail de la chevelure, netteté de l'écume et des pavés identiques). **Réglage retenu et déjà câblé dans les trois templates : LoRA ON + `steps: 8`** (`BasicScheduler`), soit 2,5× moins d'étapes que la référence à qualité équivalente. Ne pas exposer 4 steps dans l'UI **avec ce LoRA** ; plancher acceptable pour un éventuel mode brouillon = 6. Leçon générale : un sous-échantillonnage de LoRA turbo ne se détecte pas sur un critère « la scène est-elle la bonne » (elle l'est) — il faut regarder la saturation, les dégradés du ciel et **les bords du cadre**. **Voir addendum ci-dessous** : ce constat reste vrai pour la LoRA d'origine, mais n'est plus une contrainte UI absolue depuis l'ajout d'un second LoRA dédié aux 4 steps.

- **Piège n°12 bis — addendum (2026-09-08) : un second LoRA, entraîné pour 4 steps, permet d'exposer 4 steps sans le défaut de colorimétrie ci-dessus, mais seulement pour t2v/i2v** : le piège n°12 ci-dessus reste une mesure exacte et non révisée de la LoRA d'origine (`H3/minimax_h3_turbo_v4_step600_ema_pruned_comfyui.safetensors`) — ce défaut à 4 steps est toujours réel avec ce LoRA-là, et rien ne change à ce sujet. Un LoRA différent, `H3/minimax_h3_fl2v_turbo_4step_v1.2_768p_comfyui_bf16.safetensors` (nouvelle constante JS `MINIMAX_FL2V_LORA_4STEP`), spécifiquement entraîné pour tourner à 4 steps, a été validé par rendu réel comme **ne présentant pas** la dérive de colorimétrie décrite ci-dessus (aplats clippés, dominante cyan, ciel postérisé, traînée arc-en-ciel côté r2v). Il porte le nom `fl2v` car il n'est compatible qu'avec le checkpoint transformer `*_fl2va_*` utilisé par t2v/i2v — **pas** avec `*_ref2va_*` (r2v), les deux checkpoints n'étant pas interchangeables (voir plus bas dans ce même LOT). Conséquence pratique : 4 steps est désormais **exposé dans l'UI** (Canvas d'abord, puis Studio — `applyMinimaxTurbo(graph, turboOn, steps)`, `steps===4` bascule automatiquement sur ce LoRA de secours) pour t2v/i2v uniquement, **jamais** en faisant tourner la LoRA d'origine à 4 steps (ce cas précis reste cassé, cf. ci-dessus) et **jamais** pour r2v (pas de sélecteur de steps côté r2v, qui reste turbo-8-steps-ou-OFF).

- **Piège n°13 — un test FLF2V réduit à 25 frames entre deux cadrages éloignés produit une coupe franche, pas un morphing** : sur `lot1/ltx25_flf2v_00001_.mp4` (25 frames), les deux guides sont bien honorés (f0 = `lot1_scene_a.png` gros plan crépusculaire, f24 = `lot1_scene_b.png` plan large au coucher de soleil) mais l'extraction des frames intermédiaires 15/18/21 (`validate/lot1/flf_x{15,18,21}.png`, planche `flf_transition.png`) montre un **basculement instantané entre f15 et f18** : la f15 est encore le gros plan intact, la f18 est déjà le plan large intact. Il n'y a pas assez de frames latentes à cette longueur pour interpoler entre deux compositions aussi différentes. **Ne pas conclure à une régression du template** sur cette seule base : le critère de validation d'un FLF2V réduit est « les deux guides sont-ils respectés et non recadrés », pas « la transition est-elle fluide » — cette dernière ne se juge qu'à durée nominale. Corollaire de méthode : sur un FLF2V, toujours extraire **au moins une frame de part et d'autre du milieu** (pas seulement 0/mid/last), sinon on ne voit ni la coupe ni son emplacement.

- **Intégration UI LOT 1 (index.html)** : le toggle turbo Minimax H3 (`applyMinimaxTurbo`) ne cible aucun id de nœud en dur — il cherche le premier `LoraLoaderModelOnly` du graphe, retrouve sa source `model` et rebranche tout nœud qui la référence, ce qui suffit à couvrir les 3 templates (t2v/i2v/r2v) malgré des ids différents (134/121/145) sans aucune branche par pipeline ; la fonction est un no-op silencieux sur tout graphe sans cette LoRA (Krea 2, LTX 2.5, Qwen-Edit), donc appelable sans condition sur `wf.pipeline` dans le handler Generate générique. Vérifié en rendu réel (2026-09-01) : job turbo OFF sur `minimax_h3_t2v.json` accepté sans `node_errors`, `steps:20` confirmé, et le rendu prend ≈2,5× plus longtemps que le job turbo ON équivalent (75 s d'écart observé), cohérent avec 20 vs 8 steps. — Par ailleurs le toggle "Générer l'audio" (`applyLtxAudio`) devient un no-op pour LTX 2.5 et Minimax H3 : leurs templates câblent déjà `CreateVideo.audio`, et `applyLtxAudio` retourne immédiatement si ce champ est déjà rempli — aucun changement de câblage n'était donc nécessaire côté handler, seul le libellé du toggle a été généralisé. — Le remplacement de Flux2/Ernie/Z-Image par Krea 2 sur le pipeline text2image générique n'a nécessité **aucun code JS** : ces 4 templates partagent exactement les mêmes placeholders (`{{PROMPT}} {{WIDTH}} {{HEIGHT}} {{SEED}} {{BATCH}}`) et passent tous par le chemin générique fichier→`buildGraph` déjà en place ; seul `manifest.json` a changé. Les builders JS `addFluxShot`/`addErnieShot` restent utilisés ailleurs (campagne / charsheet-locsheet) et n'ont pas été touchés.

- **Le r2v Minimax H3 tient personnage ET décor simultanément, sans le piège de géométrie du dual Qwen-Edit** : `MiniMaxH3ReferenceToVideo` (`api/minimax_h3_r2v.json`, node `136`, `ref_image_size:"match"`) reçoit ses deux références en conditionings **symétriques** — `ref_images.ref_image_0` = `{{IMAGE}}` (charsheet) et `ref_images.ref_image_1` = `{{IMAGE2}}` (locsheet) — et **aucune des deux ne fournit le latent de départ** (le `latent_image` du `SamplerCustomAdvanced` vient du slot 1 du nœud lui-même). Contrairement au dual Qwen-Edit (piège n°10, où l'image 1 pilote seule la géométrie via `VAEEncode`), il n'y a donc ici ni image « dominante » ni ordre à respecter. Validé en rendu réel : sur `h3_r2v_face8_00001_.mp4` le visage correspond trait pour trait au portrait de `lot1/charsheet_00001_.png` (pixie blanc argenté, taches de son, yeux gris-bleu, ciré jaune, écharpe rouge tricotée) **pendant que** le décor reproduit `lot1/locsheet_00001_.png` (pierre blanche, fenêtre cintrée à petits carreaux, carrelage vert, colonne de laiton, escalier hélicoïdal en fonte, lanterne murale) — sur les trois frames 0/19/38, sans collage ni split-screen. Attention en revanche à ne pas confondre les checkpoints : le r2v utilise `diffusion_models/minimax_h3_ref2va_pruned_w4a8_mixed.safetensors` (11 Go), le t2v et l'i2v utilisent `minimax_h3_fl2va_pruned_w4a8_mixed.safetensors` (12 Go) — noms proches (`ref2va` vs `fl2va`), modèles différents.

## Navigation par profils métiers (LOT 2 UI) — 2026-09-01

- **Piège n°14 — dérive des noms de modèles sur disque : plusieurs pipelines historiques sont DÉJÀ cassés, indépendamment de toute modification UI**. En qualifiant la nouvelle nav par profils, trois soumissions ont été rejetées en 400 `prompt_outputs_failed_validation` — et le même rejet, avec les **mêmes `node_errors`**, se reproduit à l'identique par le chemin de navigation historique (clic direct sur l'onglet scénario, aucun profil touché), ce qui prouve que la cause est le disque, pas l'UI :
  - `workflows/api/qwen_edit_i2i.json` → `lora_name: 'Qwen-Image-Edit-2509-Lightning-4steps-V1.0-bf16.safetensors' not in (...)` : la LoRA existe toujours mais a été **déplacée dans le sous-dossier `loras/Qwen/`** ; ComfyUI attend donc `Qwen/Qwen-Image-Edit-2509-Lightning-4steps-V1.0-bf16.safetensors`. Casse tout le scénario **Localized Assets** (pipeline `image2image`).
  - `buildCampaignFullGraph` (`addFluxShot`) → `unet_name: 'flux-2-klein-9b-fp8.safetensors'` absent : sur disque on n'a plus que `flux-2-klein-9b-kv-fp8`, `flux-2-klein-9b-nvfp4` et `flux-2-klein-base-9b-fp8`. Et `addLtxShared` → `text_encoder: 'gemma_3_12B_it_fp4_mixed.safetensors'` absent (reste `gemma_3_12B_it_fp8_scaled` / `gemma4-12b-with-proj-ltx-2.5-…`). Casse le pipeline **`campaign_full`**.
  - `addErnieShot` (charsheet/locsheet, utilisé par `storyboard_v2` ET `generateReference2Video`) → `unet_name: 'ernie-image.safetensors'` absent : seul `ernie-image-turbo.safetensors` subsiste. Le job r2v n'est donc jamais atteint : la chaîne casse dès la planche personnage.
  Vérification complémentaire : le **template `api/minimax_h3_r2v.json` lui-même est sain** — POST direct du graphe avec les mêmes placeholders que `buildGraph()` → 200, `prompt_id` `da45d6fc-3fba-44ca-b3f8-c9b425d3263d`, `node_errors: {}`. Le seul verrou du pipeline `reference2video` est donc `ernie-image.safetensors`. Correctifs = une ligne par référence dans les builders JS / templates (hors périmètre du lot nav). **Leçon de méthode** : avant d'imputer une régression à une modification UI, rejouer exactement le même job par le chemin de navigation d'origine et comparer les `node_errors` — ici cela a évité d'attribuer à la nav trois pannes de packaging modèle.

  **Correctif appliqué le 2026-09-01** — 3 des 4 références cassées corrigées et validées par rendu réel réduit (méthode `docs/TESTING.md`), la 4ᵉ documentée comme non résolue :
  - **LoRA Qwen-Edit** (`workflows/api/qwen_edit_i2i.json` L105, `workflows/api/qwen_edit_dual.json` L109) : `lora_name` → `"Qwen/Qwen-Image-Edit-2509-Lightning-4steps-V1.0-bf16.safetensors"` (préfixe sous-dossier, fichier confirmé présent sous `loras/Qwen/`). **Validé** : `python3 tools/validate.py workflows/api/qwen_edit_i2i.json --image charsheet_00001_.png` → `node_errors: {}`, rendu `output/studio/edit_00006_.png` inspecté — planche personnage cohérente (combinaison spatiale blanche/orange, 3 vues), aucune dérive ni artefact.
  - **`addFluxShot`** (`index.html` L3065) : `unet_name` → `"flux-2-klein-9b-kv-fp8.safetensors"`. **Attention** : le candidat "le plus proche par le nom" (`flux-2-klein-base-9b-fp8.safetensors`, essayé en premier) produit un rendu visuellement cassé (bruit haute fréquence, texte totalement illisible, visage déformé — cf. `flux_klein_base_test_00001_.png`) malgré un statut `success` et `node_errors: {}` — nouvelle confirmation qu'un statut succès ne garantit rien sur le contenu. `flux-2-klein-9b-kv-fp8.safetensors` (même encodeur `qwen_3_8b_fp8mixed`, aucun changement d'appariement requis) produit une image nette et cohérente (`flux_klein_kv_test_00001_.png`). **Validé en conditions réelles** : job `campaign_full` complet soumis via `POST /prompt` (77 nœuds, posters+thumbnail+social+teaser LTX) → `node_errors: {}`, `status_str: success`, 4 fichiers produits sous `output/studio/campaign/` — poster, thumbnail et social inspectés (même personnage astronaute/décor ruines-jungle cohérent sur les 3, titres textuels illisibles mais c'est une limite connue de génération de texte par le modèle, pas un artefact de rendu).
  - **`addLtxShared`** (`index.html` L2960) et templates `ltx_t2v.json`/`ltx_i2v.json` (L485) : `text_encoder` → `"gemma_3_12B_it_fp8_scaled.safetensors"`. Chemin de code confirmé encore actif : seul appelant `buildFLF2VGraph` (séquence FLF2V manuelle), et `ltx_t2v.json`/`ltx_i2v.json` restent utilisés par `buildCampaignFullGraph` (teaser) et par les pipelines génériques text2video/image2video du manifest. **Validé** : `python3 tools/validate.py workflows/api/ltx_t2v.json --reduce --frames 0,12,24 --audio` → rendu réduit 25 frames + audio 29,7 Ko, 3 frames extraites et inspectées (forêt enneigée au lever du soleil, mouvement de caméra cohérent, aucun artefact) ; confirmé une seconde fois dans le contexte réel du teaser `campaign_full` (`teaser_00006_.mp4`, 704×1280, 25 frames/1 s) — frames 0 et 24 inspectées, astronaute gravissant un escalier de temple envahi par la végétation, mouvement fluide, aucune dérive de contenu.
  - **`addErnieShot`/`addErnieShared` (`index.html` L3103/L3111) — RÉSOLU le 2026-09-01, en dehors du lot correctif ci-dessus** : `unet_name` → `"ernie-image-turbo.safetensors"` (L3103) **et** `steps: 20, cfg: 4` → `steps: 8, cfg: 1` sur le `KSampler` de `addErnieShot` (L3111-3112), pour aligner sur le réglage turbo déjà validé et utilisé ailleurs dans ce même projet (`workflows/api/ernie_turbo_t2i.json`, dont le `CLIPLoader`/`VAELoader` — `ministral-3-3b` + `flux2-vae` — correspondaient déjà exactement à `addErnieShared`, seuls `unet_name` et les réglages du sampler différaient). **Validé en rendu réel** : graphe `addErnieShared`+`addErnieShot` reproduit tel quel via `POST /prompt` direct (charsheet 1920×1088, prompt turnaround personnage ciré jaune/écharpe rouge/bottes vertes) → `node_errors: {}`, `status_str: success`, image inspectée (`studio/lot6_erniefix_test_00001_.png`) : planche 5 vues (face/3-4/profil/dos/portrait) pleine cadre, sans texte ni collage, personnage cohérent d'une vue à l'autre. La chaîne `reference2video` (charsheet → locsheet → `minimax_h3_r2v`) est désormais débloquée de bout en bout — le nœud `MiniMaxH3ReferenceToVideo` était déjà sain (piège n°14 ci-dessus).

- **Piège n°15 — une capture headless avec `--virtual-time-budget` peut figer une frame ANTÉRIEURE à l'état DOM final**. En capturant l'état « profil sélectionné » via une page helper same-origin (iframe + `.click()` différé), le `--screenshot` montrait la carte de profil active MAIS l'onglet scénario resté sur l'état initial, alors qu'un `--dump-dom` sur *la même URL et le même budget* renvoyait l'état correct (`TAB:storyboard CARD:director PIPE:storyboard_v2`). Autrement dit la capture et le dump ne s'échantillonnent pas au même instant, et le rendu peut être en retard d'un ou deux repaints sur le DOM. **Ne jamais conclure à un bug de l'app sur la seule foi d'un pixel de capture** : croiser systématiquement avec un `--dump-dom` du même scénario. Contournement qui a fonctionné : déclencher le clic plus tôt (≈3 s après le `load` de l'iframe au lieu de 4 s) et augmenter le budget de temps virtuel (20 s), pour laisser plusieurs repaints après la mutation du DOM. Écrire aussi l'assertion dans `document.title` de la page helper permet de valider l'état et de capturer dans la même passe.

## Bascule intégrale sur Krea 2 / LTX 2.5 / Minimax H3 (LOT 7) — 2026-09-01

- **Piège n°16 (CRITIQUE pour tout portage vers Krea 2) — `ConditioningZeroOut` : le prompt négatif n'est pas « faible », il est TOTALEMENT ABSENT.** Dans `workflows/api/krea2_t2i.json` (et donc dans les builders JS `addKrea2Shared`/`addKrea2Shot` qui en sont la copie), l'entrée `negative` du `KSampler` ne vient PAS d'un `CLIPTextEncode` du texte négatif mais d'un `ConditioningZeroOut` appliqué au **conditioning positif** : le texte négatif n'est jamais encodé, il n'existe pas dans le graphe. C'est une différence de nature avec le cas déjà connu « cfg 1 → negative ignoré » (LTX distillé, Qwen-Edit Lightning, piège n°5/n°7) où le négatif est au moins encodé et pourrait redevenir actif en montant le cfg : ici, monter le cfg ne changerait rien. **Conséquence pour tout portage depuis Ernie/Flux2** : un négatif riche qui « fonctionnait » (ex. `CHARSHEET_NEG`/`LOCSHEET_NEG` à cfg 4 sur Ernie) devient un no-op silencieux — le graphe passe, le job réussit, et le défaut (texte incrusté, cadres de planche, collage, personnes parasites sur une fiche décor) ne se voit qu'à l'inspection des pixels. **La seule parade est de tout reformuler en affirmatif dans le prompt**, exactement comme pour `keyframePrompt()` au piège n°7. Implémentation retenue : la constante `SHEET_CLEAN` (index.html), formulée en positif — « One single seamless uncut photograph filling the whole frame edge to edge, completely free of any text… with no drawn borders, no panel lines… Pure photography: real photographic capture, sharp focus, high detail, correct anatomy, clean seamless background » — plus, pour la fiche décor, « The location is completely empty and deserted » à la place du bannissement négatif des personnes. **Validé en rendu réel du premier coup** (aucune itération de prompt nécessaire) : `output/studio/story/charsheet_00011_.png` (planche pleine cadre, turnaround 4 poses face/¾/profil/dos + grand portrait + rangée de 6 expressions + gros plans costume + bandeau de 8 nuances, personnage identique d'une vue à l'autre, zéro texte/légende/cadre) et `locsheet_00007_.png` (3 plans larges multi-angles + rangée de 5 plans moyens + rangée de gros plans matières, aucun humain, zéro texte). Le paramètre `negText` reste dans la signature de `buildCharsheetGraph`/`buildLocsheetGraph` mais est **sans effet** — documenté en commentaire pour éviter qu'un futur lot croie à un bug de câblage.

- **La chaîne FLF2V multi-segments se porte sur LTX 2.5 sans nouveau template** : `ltx25_flf2v.json` ne couvre qu'un segment (2 images guides), mais son bloc de sampling est **répétable tel quel** par segment depuis le builder JS. `addFLF2VChain` a donc été migré en gardant sa structure (boucle sur les segments, `ImageBatch` pour les images, `AudioConcat(direction:"after")` pour l'audio) et en ne changeant que le contenu du bloc : `LTXVDualCFGGuider(video_cfg 1, audio_cfg 1)` au lieu de `CFGGuider(cfg 1)`, `img_compression 18` au lieu de 35, `VAEDecodeTiled(512/64/64/16)` au lieu de 768/64/4096/64, VAE vidéo et VAE audio **séparés** (`ltx-2.5-video-vae-bf16` / `ltx-2.5-audio-vae-bf16`, deux `VAELoader`) au lieu de l'audio VAE embarqué dans le checkpoint 2.3, et loaders partagés (`UNETLoader` + 2 `CLIPLoader`) au lieu de `CheckpointLoaderSimple`+`LTXAVTextEncoderLoader`. Les slots ne changent pas (sortie sampler **slot 1** = `denoised_output`, `LTXVCropGuides` slot 2). **Validé en rendu réel sur 2 segments enchaînés** (`output/studio/sequence_00008_.mp4`, 3 images guides, 832×448) : 50 frames = exactement 2×25, audio aac 48 kHz stéréo 2,02 s **sans aucun silence détecté** (`silencedetect -40dB:0.3` → 0 occurrence, la concaténation audio inter-segments est donc propre), et les 3 guides sont honorés dans le bon ordre — f0 = `lot1_scene_a.png` (gros plan, phare turquoise), f24 = `lot1_scene_b.png` (plan large, phare à lanterne rouge, fin du segment 1), f49 = `lot7_key.png` (observatoire de montagne, fin du segment 2). Aucun recadrage brutal (`ImageScale(crop:"center")` amont conservé, piège n°3 toujours neutralisé).

- **`applyLtxAudio` n'a plus lieu d'être** : LTX 2.5 comme Minimax H3 câblent `CreateVideo.audio` nativement dans leurs templates, et la chaîne FLF2V JS produit son audio elle-même. La fonction (et son appel dans le handler Generate) a été supprimée plutôt que laissée en no-op. Idem `addFluxShared`/`addFluxShot`/`addErnieShared`/`addErnieShot`/`addLtxShared`/`addLtxEnhance`, sans appelant restant.

- **Piège de fps silencieux au portage 2.3 → 2.5 : LTX 2.5 rend en 24 fps, LTX 2.3 en 25.** Trois endroits calculaient ou déclaraient 25 en dur côté JS : la formule de frames de `addFLF2VChain` (`8*round((durée*25-1)/8)+1`), le `CreateVideo.fps` de `addVideoOutput` (chaîne FLF2V) et celui de `buildAnimaticGraph` (assemblage des cuts du storyboard). Laisser 25 sur des cuts rendus à 24 fps ne casse rien visiblement — le job réussit — mais **désynchronise progressivement l'audio** de l'assemblage (~4 % de dérive) et raccourcit la vidéo par rapport à sa piste son. Constante `LTX25_FPS = 24` introduite et utilisée aux trois endroits. Vérifié sur les rendus : teaser campagne, cut i2v et chaîne FLF2V ressortent tous en `r_frame_rate=24/1` avec une piste audio de durée cohérente.

- **Qualification LOT 7 (rendus réels, 2026-09-01)** — les 4 remplacements, chacun inspecté visuellement :
  - **Campagne** (`buildCampaignFullGraph`, Krea 2 + teaser `api/ltx25_t2v.json`) : job complet 63 nœuds accepté, 4 sorties sous `output/studio/campaign/` — `poster_00008_.png` (832×1216 : dôme d'observatoire en ruine, explorateur en silhouette, palette teal/rouille conforme au brief, titre « LONE EXPLORER » **lisible**), `thumbnail_00008_.png` (1280×720, composition YouTube), `social_00007_.png` (1024×1024). Le texte secondaire reste partiellement brouillé (limite connue des modèles d'image, pas un artefact de rendu) mais Krea 2 produit un titre nettement plus lisible que Flux2 sur le même gabarit. Teaser `teaser_00007_.mp4` : 704×1280, 24 fps, 25 frames, audio aac 48 kHz stéréo 1,01 s ; frames 0 et 23 inspectées — explorateur traversant une structure métallique en ruine, recul de caméra cohérent, aucune dérive.
  - **Planches charsheet/locsheet** : voir piège n°16 ci-dessus (validées du premier coup).
  - **Cut i2v du storyboard** (`api/ltx25_i2v.json`, placeholders identiques à `ltx_i2v.json` → aucune adaptation de `submitCutJob`) : `output/studio/story/cut_lot7_00001_.mp4`, 832×448, 24 fps, 25 frames, audio 1,01 s. **Frame médiane (f12) inspectée** : le personnage (veste de campagne olive, boussole de laiton, cheveux bouclés noirs) ET le décor (tour de béton fissurée, escalier d'acier rouillé, fenêtres cintrées brisées, blocs couverts de lichen) de la keyframe guide sont intacts au milieu du clip — l'ancrage scène+personnage+décor du piège n°9 tient sur LTX 2.5. Bonus : contrairement au 2.3, le `TextGenerateLTX2Prompt` de LTX 2.5 accepte une **entrée image** (câblée sur le `LTXVPreprocess` du guide dans `addFLF2VChain`), ce qui attaque la racine même du piège n°9 au lieu de la compenser par le prompt.
  - **Chaîne `reference2video` complète** (charsheet Krea 2 → locsheet Krea 2 → `api/minimax_h3_r2v.json`) : `output/studio/minimax_h3_r2v_00002_.mp4`, 832×480, 24 fps, 39 frames (grille 17n+5, piège n°11 confirmé). Frames 19 et 38 inspectées — le personnage correspond trait pour trait à `charsheet_00011_.png` (peau brune, boucles noires, veste de campagne olive sur pull gris, boussole de laiton au cou, sacoche de toile, gants sans doigts) **pendant que** le décor reproduit `locsheet_00007_.png` (tour de béton, escaliers d'acier rouillé, fenêtres cintrées à carreaux brisés, rochers à lichen jaune, crête de montagne, lumière d'aube froide), sans collage ni split-screen. La cohérence personnage+décor du r2v H3 est donc conservée à l'identique avec des planches Krea 2 au lieu d'Ernie.
